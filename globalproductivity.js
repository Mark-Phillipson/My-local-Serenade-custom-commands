// //This command does not seem to work, need a way to permanently press shift
// serenade.global().command("<%number%> items", async (api,matches) => {
// 	const increments=parseInt(matches.number);
// 		await api.pressKey("down",["shift"],increments);
// 		await api.wait( 5000);
// })